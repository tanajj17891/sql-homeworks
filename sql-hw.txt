use sql_store;

-- question 1 

select *

from customers

order by state ASC 


-- question 2


SELECT SUM(points) FROM customers;

-- question 3 

select customer_id, -- what we want in the output
count(*) as order_count -- counts the rows with the same values 
from orders -- our table
group by customer_id -- groups row with same values in customer_id 

use sql_store;

-- question 4

SELECT
  first_name, last name, birth_date, -- what we want the output to look like
  
  TIMESTAMPDIFF(YEAR, birth_date, CURDATE()) AS age -- curdate = today's date , timestampdiff checks if birthday has happened this year already 

from customers -- table name 

question -5 
select shipper_id, count(*) from orders where status = 2 group by shipper_id order by desc limit 1;

